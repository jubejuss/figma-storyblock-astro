# Storyblok API tokens
# Hangi: https://app.storyblok.com → Space → Settings → Access Tokens
STORYBLOK_DELIVERY_API_TOKEN=preview_or_public_token_here

# Space region (eu / us / ap / ca / cn)
STORYBLOK_REGION=eu

# Space ID (CLI ja typegen jaoks)
STORYBLOK_SPACE_ID=000000
